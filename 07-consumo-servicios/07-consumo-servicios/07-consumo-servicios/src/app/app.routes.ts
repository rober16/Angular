import { Routes } from '@angular/router';
import { FactorialComponent } from './main/pages/factorial/factorial.component';

export const routes: Routes = [
  { path: '', component: FactorialComponent }
];
