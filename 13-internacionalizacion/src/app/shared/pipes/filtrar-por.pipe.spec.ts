import { FiltrarPorPipe } from './filtrar-por.pipe';

describe('FiltrarPorPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarPorPipe();
    expect(pipe).toBeTruthy();
  });
});
